{
  "name": "quire",
  "version": "0.1.0",
  "private": true,
  "description": "Quire — export Figma sections as ordered, compressed PDFs",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "pdf-lib": "^1.17.1",
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.2",
    "esbuild": "^0.24.0",
    "jpeg-js": "^0.4.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
